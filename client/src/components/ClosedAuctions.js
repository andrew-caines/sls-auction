export default function ClosedAuctions(props) {
    return (<div>This would list all auctions, with the STATUS of closed that are still on the server and not auto-deleted after 1 day of being closed.</div>)
}